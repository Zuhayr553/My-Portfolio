name: flutter_profile
description: Zuhayr's Portfolio

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^5.1.0
  flutter_svg: ^0.22.0
  animated_text_kit: ^4.2.1
  url_launcher: ^6.0.0
  firebase_core: ^2.7.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any


flutter:

  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/

